import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Scan = () => {
  return (
    <View className="bg-white h-screen w-screen flex items-center justify-center">
      <Text className="text-3xl font-bold text-blue-900">Scan page is Under progress</Text>
    </View>
  )
}

export default Scan

const styles = StyleSheet.create({})